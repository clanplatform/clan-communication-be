SERVICE_NAME=email-service
VERSION=1.0.0
DEBUG=false
PORT=8001

DATABASE_URL=postgresql+asyncpg://clan:clan_dev@postgres:5432/emails
REDIS_URL=redis://redis:6379/0

# ── SendGrid (primary) ────────────────────────────────────────────────────────
# Sign up free at https://sendgrid.com → Settings → API Keys → Create API Key
# Free tier: 100 emails/day forever
SENDGRID_ENABLED=true
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── SMTP (fallback — used if SendGrid fails or SENDGRID_ENABLED=false) ────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USERNAME=your@gmail.com
SMTP_PASSWORD=xxxx-xxxx-xxxx-xxxx
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=Clan Platform

# ── AWS SES (optional second fallback) ───────────────────────────────────────
AWS_SES_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SES_ENABLED=false

# ── Internal services ─────────────────────────────────────────────────────────
NOTIFICATION_SERVICE_URL=http://notification-service:8000
